name: Cancel outdated waiting workflows

on:
  workflow_dispatch:
  schedule:
    - cron: "24 6 * * MON-FRI" # Every weekday at 06:24 UTC

jobs:
  cancel-waiting-workflows:
    permissions:
      contents: read
      actions: write
    name: Cancel outdated waiting workflows
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/cancel_outdated_waiting_workflows.yml@pgp-SDIT-3059-cancel-outdated # WORKFLOW_VERSION
    secrets: inherit
